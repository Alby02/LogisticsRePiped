# Immediate TODOs

This file is for near-future tasks that are specific to the current development cycle, separate from the high-level roadmap.

- [ ] **Robust Pipe Connections**: Currently, wrench modifications only toggle the visual blockstate. Move connection state saving to a `BlockEntity` to ensure it persists robustly across block updates and chunk loads.
- [ ] **Pipe Rendering**: Replace placeholder 1-pixel frame mathematical textures with final art assets once available.
- [ ] **Data Generation**: Add automated datagen for pipe models and blockstates instead of manual JSON files if the project scales up.
